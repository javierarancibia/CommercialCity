<%= form_with(url: update_visit_path, local: true) do |form| %>
    <% if visit.errors.any? %>
        <div id="error_explanation">
        <h2><%= pluralize(visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

        <ul>
        <% visit.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
        </ul>
        </div>
    <% end %>

    <div class="field">
        <%= form.label :content %>
        <%= form.text_field :content %>
    </div>
    <div>
          <%= form.label :start, "Inicio" %>
          <%= form.datetime_field :start %>
      </div>
    <div>
        <%= form.label :end, "Termino" %>
        <%= form.datetime_field :end %>
    </div>

    <div class="actions">
        <%= form.submit %>
    </div>

<% end %>