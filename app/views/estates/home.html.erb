  
  
  <div class="hero-image">
    <!-- Navbar de usuario -->
    <% if user_signed_in? %>
        <%= link_to  "Mis propiedades", estate_dashboard_path, class: "mt-3 ml-3 btn btn-primary text-white" %>
        <%= link_to  "Mis Favoritos", estate_favorite_path, class: "mt-3 ml-3 btn btn-primary text-white" %>
        <span class="ml-3"><%= current_user.first_name %></span>
      <% end %>
    <% if current_user %>
        <%= link_to  "Cerrar Sesion", destroy_user_session_path, method: :delete, class: "mt-3 ml-3 btn btn-primary text-white" %>
        <%= link_to  "Edita tu perfil", edit_user_registration_path, class: "mt-3 ml-3 btn btn-primary text-white" %>
    <% else %>
        <%= link_to  "Inicia Sesion", new_user_session_path, class: "mt-3 ml-3 btn btn-primary text-white" %>
        <%= link_to  "Registrate", new_user_registration_path, class: "mt-3 ml-3 btn btn-primary text-white" %>
    <% end %>

    <!-- Main jumbotron for a primary marketing message or call to action -->
    <div class="hero-text">
      <h1>CommercialCity</h1>
      <p>La comunidad digital del mundo inmobiliario</p>
      <p><a class="btn btn-primary btn-lg" href="#" role="button">Learn more »</a></p>

      <%= render 'search_form' %>
    </div>
  </div>




  <div class="container">
    <!-- Example row of columns -->
    <hr>

<!-- Contador de tipos de propiedad con sus logos-->

<% @categories.each do |cat| %>
  <%= cat.name %>
  <%= cat.estates.count %>
<% end %>



<!-- Muestra al azar de 6 propiedades publicadas. Cambian al hacer refresh a la pagina -->    
<h2>Algunas propiedad publicadas</h2>
<div class="row row-cols-1 row-cols-md-3">
  <% @estates_random.each do |estate| %>
      <div class="col-md-4">
        <div class="card h-100">
          <%= image_tag estate.photos[0], class: 'card-img-top', width: 300 if estate.photos.attached?%>
          <div class="card-body">
            <h2 class="card-title"> <%= estate.address %></h2>
            <p class="card-text">Size: <%= estate.rooms %><br>$<%= estate.price %></p><%= estate.rooms %>
          </div>
        </div>
      </div>
  <% end %>
</div>

